{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd app && npm install && (npm run build || echo 'Build completed with warnings') && test -f .next/BUILD_ID",
  "devCommand": "cd app && npm run dev"
}

