{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd app && npm install && npm run build; if [ -f .next/BUILD_ID ]; then exit 0; else exit 1; fi",
  "devCommand": "cd app && npm run dev"
}

